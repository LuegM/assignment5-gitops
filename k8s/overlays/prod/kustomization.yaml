apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: todo-prod

resources:
  - namespace.yaml
  - ../../base

namePrefix: prod-

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: test-nginx
